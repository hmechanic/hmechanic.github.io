dist
pnpm-lock.yaml
public/cv

# CV content: hand-edited multiline strings that Prettier's YAML parser
# mishandles (valid for js-yaml at runtime). Not reformatted to avoid altering
# the text the site renders.
src/content/cv/*.yaml
